<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">
    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".teacher_info"></activity>
        <activity android:name=".Register"></activity>
        <activity android:name=".find_pass" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".course_info" />
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".ac_serach"></activity>
        <activity android:name=".infoactivity"></activity>
        <activity android:name=".prepactivity"></activity>
        <activity android:name=".setactivity"></activity>
        <activity android:name=".add_class"></activity>
        <activity android:name=".ac_classify"></activity>
        <activity android:name=".MainActivity1"></activity>
        <activity android:name=".EditScheduleActivity"></activity>
        <activity android:name=".interest"></activity>
    </application>

</manifest>